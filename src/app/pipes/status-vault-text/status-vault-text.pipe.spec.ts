import { StatusVaultTextPipe } from './status-vault-text.pipe';

describe('StatusVaultTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusVaultTextPipe();
    expect(pipe).toBeTruthy();
  });
});
